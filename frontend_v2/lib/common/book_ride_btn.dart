import 'package:flutter/material.dart';

class BookRideBtn extends StatelessWidget {
  const BookRideBtn({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
